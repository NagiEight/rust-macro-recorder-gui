// components/GraphSidePage.jsx
import React from "react";

/**
 * A placeholder page for a graph visualization.
 * It will display a simple heading and text.
 */
const GraphSidePage = () => {
  return (
    <div className="p-2 text-gray-300">
      <h2 className="text-xs font-bold text-gray-500 uppercase mb-2">
        Graph View
      </h2>
      <p className="text-sm">
        This is the graph visualization page. We can add a graph or a data
        visualization component here later.
      </p>
    </div>
  );
};

export default GraphSidePage;
